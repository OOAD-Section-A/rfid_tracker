<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.nova</groupId>
    <artifactId>rfid-tracker</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>SCM Subsystem 11 — RFID Tracker (Team NOVA)</name>
    <description>
        Barcode and RFID tracking subsystem for the SCM Supply Chain System.
        Integrates with the shared OOAD database (DB team schema.sql).
        Falls back to scm_rfid_db for standalone/demo mode.
    </description>

    <properties>
        <!--
          Java 17 to match the DB team's pom.xml (maven.compiler.release=21 uses 21,
          but 17 is fine; set to 17 here to match original rfid pom.xml).
          Change to 21 if DB team requires it.
        -->
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <!--
          MySQL JDBC Driver.
          DB team's pom.xml uses 9.3.0.  We align to the same version
          so both subsystems use the same driver on a shared MySQL 8/9 server.
          (Our original used 8.3.0 — bumped to match the DB team.)
        -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>9.3.0</version>
        </dependency>

        <!-- SLF4J simple logger (matches DB team dependency) -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>2.0.17</version>
        </dependency>

        <!-- JUnit 5 for unit tests -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>

        <!-- DB team integration JAR -->
        <dependency>
            <groupId>com.jackfruit.scm</groupId>
            <artifactId>database-module</artifactId>
            <version>1.0.0-SNAPSHOT</version>
        </dependency>

        <!-- exception team integration JAR -->
        <dependency>
            <groupId>com.scm</groupId>
            <artifactId>scm-exception-handler</artifactId>
            <version>v3</version>
        </dependency>
        
    </dependencies>

    <build>
        <finalName>rfid-tracker</finalName>

        <plugins>
            <!-- Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
            </plugin>

            <!-- Surefire for JUnit 5 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
            </plugin>

            <!-- Fat JAR — includes all dependencies (MySQL driver + SLF4J) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.6.0</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>com.nova.rfid.RFIDTrackerApp</mainClass>
                        </manifest>
                    </archive>
                    <descriptorRefs>
                        <descriptorRef>jar-with-dependencies</descriptorRef>
                    </descriptorRefs>
                    <finalName>rfid-tracker</finalName>
                    <appendAssemblyId>true</appendAssemblyId>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- exec-maven-plugin to run directly via mvn exec:java -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <mainClass>com.nova.rfid.RFIDTrackerApp</mainClass>
                    <additionalClasspathElements>
                        <additionalClasspathElement>${project.basedir}/lib/scm-exception-handler-v3.jar</additionalClasspathElement>
                    </additionalClasspathElements>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>